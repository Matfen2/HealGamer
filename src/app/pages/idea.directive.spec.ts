import { IdeaDirective } from './idea.directive';

describe('IdeaDirective', () => {
  it('should create an instance', () => {
    const directive = new IdeaDirective();
    expect(directive).toBeTruthy();
  });
});
